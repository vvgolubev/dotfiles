Config { 

   -- appearance
     font =         "xft:Fira Mono:size=9:bold:antialias=true"
   , bgColor =      "black"
   , fgColor =      "#646464"
   , position = Static { xpos = 0 , ypos = 0, width = 1900, height = 25 }
   , border =       BottomB
   , borderColor =  "#11111100"

   -- layout
   , sepChar =  "%"   -- delineator between plugin names and straight text
   , alignSep = "}{"  -- separator between left-right alignment
   , template = "| %kbd% | %date% | %vol% | %StdinReader% }{"

   -- general behavior
   , lowerOnStart =     True    -- send to bottom of window stack on start
   , hideOnStart =      False   -- start with window unmapped (hidden)
   , allDesktops =      True    -- show on all desktops
   , overrideRedirect = True    -- set the Override Redirect flag (Xlib)
   , pickBroadest =     False   -- choose widest display (multi-monitor)
   , persistent =       True    -- enable/disable hiding (True = disabled)

   , commands = 
        [ 
        -- time and date indicator 
          Run Date           "<fc=#ABABAB>%F (%a) %T</fc>" "date" 10

        -- volume
        , Run PipeReader "/tmp/.volume-pipe" "vol"

        -- keyboard layout indicator
        , Run Kbd            [ ("us", "<fc=#429942>US</fc>"),
                               ("ru", "<fc=#781A0F>RU</fc>")
                             ]
        -- status
        , Run StdinReader
        ]
   }